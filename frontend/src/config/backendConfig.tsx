const backendURL = "http://localhost:8080/api";
const websocketserverAddress = "http://localhost:8000";
const jwtStorageName = "jwt_token";

const config = { backendURL, jwtStorageName, websocketserverAddress };

export default config;
